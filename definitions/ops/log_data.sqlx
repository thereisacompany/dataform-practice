config {
  type: "table",
  name: "log_data",
  schema: "dataform",
  tags: ["ops", "logging"]
}

-- 空殼 log 表；實際寫入可用 helpers.logSQL() 放 post_operations
SELECT
  CURRENT_DATE() AS execution_date,
  CURRENT_TIMESTAMP() AS execution_time,
  "初始化建表" AS description
WHERE FALSE
